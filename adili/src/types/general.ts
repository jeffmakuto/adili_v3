export type LearnMoreButtonProps = {
    onClick: () => void;
}

export type SectionImageProps = {
    src: string;
    alt: string;
}

export type VideoPlayerProps = {
    className?: string;
    videoUrl: string;
}